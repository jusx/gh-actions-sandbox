name: Test If

on:
  push:
    tags:
      - '*'
    branches:
      - 'main'

jobs:
  build:
    name: Testing if
    if: ${{ secrets.TEST }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
